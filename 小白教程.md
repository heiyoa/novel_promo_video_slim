# 小白部署教程（Windows）

适用对象：几乎不懂编程，但已安装 VSCode + Kilocode + GLM4.6（或其他本地模型服务）的朋友。

---

## 1. 解压项目
1) 把压缩包解压到任意目录（例如 `D:\Projects\novel_promo_video_slim`）。
2) 确保目录内有这些文件夹：
- `config`、`modules`、`data`、`assets`、`outputs`

---

## 2. 打开项目
1) 打开 VSCode
2) 选择 “打开文件夹”，选中 `novel_promo_video_slim` 目录
3) 打开 VSCode 终端（菜单：终端 -> 新建终端）

---

## 3. 安装依赖
在终端执行：
```bash
python --version
```
如果能看到版本号，继续执行：
```bash
python -m pip install -r requirements.txt
```

> 如果提示 python 不存在，请先安装 Python 3.10+。

---

## 4. 必填配置（第一次一定要改）
打开 `config/system_config.json`，重点改这几项：

### 4.1 剪映草稿目录
```json
"paths": {
  "draft_folder": "E:/jianying/JianyingPro Drafts"
}
```
把它改成你电脑真实的剪映草稿目录。

### 4.2 Edge 浏览器信息（用于抓推广信息）
```json
"edge": {
  "edge_path": "C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe",
  "edge_user_data": "C:/Edge_Dev",
  "edge_port": 9223
}
```
- `edge_user_data` 是一个“专用的 Edge 用户目录”，建议保持不变，脚本会在里面保存登录态。

### 4.3 即梦生图（必须填 session_id）
```json
"jimeng": {
  "session_id": "填你自己的 session_id"
}
```
如果不填，会提示 `jimeng session_id missing`。

### 4.4 LLM 改写模型（GLM4.6 或你的本地模型）
```json
"llm": {
  "api_key": "你的 key（没有就留空）",
  "api_endpoint": "http://127.0.0.1:8045",
  "model": "",
  "model_fallbacks": [ ... ]
}
```
- 如果你用的是 Kilocode/GLM4.6 本地服务，请把 `api_endpoint` 改成你本机实际接口地址。
- 如果不会设置，先问发你项目的人。

---

## 5. 工作流配置（建议改）
打开 `config/workflow_config.json`：

### 5.1 选择书名
```json
"book_selection": {
  "book_name": "书名",
  "book_limit": 1
}
```
你也可以在运行时用命令覆盖（见第 7 步）。

### 5.2 生图提示词
```json
"image_prompt": "这里填你的提示词"
```
这个必须填，否则会报错 `image_prompt missing`。

### 5.3 改写提示词（可选）
```json
"rewrite_prompt": "你的改写提示词"
```
不填则使用默认提示词。

---

## 6. 先登录一次推广网站
1) 用 Edge 打开 `promo_fetch.base_url`（在 `system_config.json` 中）
2) 登录账号（脚本需要登录态）
3) 不要清理 `edge_user_data` 文件夹

如果抓取失败，通常就是没登录或页面结构变了。

---

## 7. 运行命令
在项目根目录终端执行：
```bash
python run_workflow.py --book-name "书名"
```
常用参数：
```bash
python run_workflow.py --book-name "书名" --book-limit 3
python run_workflow.py --skip-promo
python run_workflow.py --skip-video
```

---

## 8. 输出在哪里
- 推广抓取结果：`outputs/promo/promo_<book>.json`
- 改写脚本：`outputs/<book>/script.md`
- 字幕文件：`outputs/<book>/subtitles.srt`
- 剪映草稿：`outputs/<book>/draft.json`
- 本次运行汇总：`outputs/job_output.json`

剪映草稿会写到你配置的 `draft_folder` 里，用剪映打开即可。

---

## 9. 常见报错和解决方法
- **jimeng session_id missing**：去 `system_config.json` 填 session_id。
- **image_prompt missing**：去 `workflow_config.json` 填 `image_prompt`。
- **search box not found / promo button not found**：网站改版，调整 `system_config.json` 的 `promo_fetch.selectors`。
- **draft folder missing**：剪映草稿目录配置不对。
- **AI 改写失败**：LLM 接口没开或地址不对，检查 `llm.api_endpoint`。

---

## 10. 迁移到新电脑
1) 复制整个 `novel_promo_video_slim` 目录
2) 重新修改 `config/system_config.json`
3) 安装依赖后运行

---

如果还是不会，截图 `outputs/job_output.json` 发给发你项目的人即可。
